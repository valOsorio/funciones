/*Convertir la siguiente funcion a arrow function
let suma = function(a, b){
    return a + b
    }
    */

/*
//esta es la arrow function completa
const suma = ( a, b) => {
    return( a + b )
}
*/

//esta es la arrow function abreviada
const suma = (a, b) => a + b;
